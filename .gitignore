node_modules/
dist/
.hf-out/

.env
.env.*
!.env.example
# Tracked on purpose: VITE_ values are compiled into the public bundle, so none
# of them are secret, and the live site is broken without this one.
!.env.production

# holds the database password
api/config.php
# uploaded covers belong on the server, not in the repo
api/uploads/*
!api/uploads/.htaccess

.claude/settings.local.json

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

.DS_Store
Thumbs.db
